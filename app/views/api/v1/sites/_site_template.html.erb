<!DOCTYPE html>
<html>
  <head>
    <meta charset='utf-8'>
    <link rel="icon" type="image/x-icon" href="<%= site.icon %>" >
    <title><%= site.title %></title>

    <meta name='viewport' content='width=device-width, initial-scale=1'>
    <style>
      body{
        background-color: <%= site.background_color %>;
      }

      .container{
        display: block;
        width: 60%;
        margin: auto;
      }

      .banner{
        height: 65px;
        padding: 11px;
        border-radius: 15px;        
      }

      .banner img{
        float: left;
        max-width: 100px;
      }

      .banner .title {
        float: left;
        margin: 14px 0px 14px 14px;
      }

      .widgets-container{
        margin-top: 50px;
      }

      .widget{
        margin-top: 20px;
        padding: 11px;
      }

      .widget h3{
        text-align: center;
      }
    </style>
  </head>
  <body >
    <div class='container'>
      <%= render 'api/v1/sites/banner_template', banner: site.banner %>
      <%= render 'api/v1/sites/widgets_template', widgets: site.widgets %>
    </div>
  </body>
</html>
